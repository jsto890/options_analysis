# Frontend

React + TypeScript client for the options ladder, signal panels, and websocket playback/live rendering.

## Run

```bash
cd frontend
npm ci
npm run dev
```

## Build

```bash
cd frontend
npm run build
```

## Test

```bash
cd frontend
npm test
```

The frontend expects the backend websocket stream and REST endpoints to be reachable on localhost.
